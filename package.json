{
  "name": "didt",
  "version": "0.0.0",
  "main": "index.js",
  "bin": "cli.js",
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "deps": "standard && dependency-check . && dependency-check . --unused --no-dev",
    "lint": "standard"
  },
  "devDependencies": {
    "dependency-check": "^3.0.0",
    "standard": "^16.0.3"
  },
  "dependencies": {
    "dotenv": "^5.0.0",
    "JSONStream": "^1.3.2"
  }
}
