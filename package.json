{
  "name": "didt",
  "version": "0.0.0",
  "main": "index.js",
  "bin": [
    "cli.js"
  ],
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=8.9.4"
  },
  "scripts": {
    "deps": "standard && dependency-check . && dependency-check . --unused --no-dev",
    "lint": "standard"
  },
  "devDependencies": {
    "dependency-check": "^3.0.0",
    "standard": "^14.3.1"
  },
  "dependencies": {
    "JSONStream": "^1.3.2",
    "dotenv": "^5.0.0"
  }
}
