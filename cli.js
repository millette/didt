'use strict'

// self
// const { doone } = require('.')
const { dothem2 } = require('.')

// doone((user) => console.log(user))
dothem2((user) => console.log(user))
  .then(console.error)
  .catch(console.error)
